# CellSeg_Extension

At first run the RUN_FIRST_TO_CREATE_DATA_FOLDER.py script. This will create the needed directories for your data.
Copy your data into their respective folders.

Afterwards run the entire image_preprocessing.ipynb notebook from top to bottom.
Then the entire CellSeg_extension.ipynb notebook, also from top to bottom.
At the Gradio_Cell_Segmentor.ipynb notebook.
